#include <ctype>

/**
 * _isdigit - checks if c is a digit (0 - 9)
 * @c: passed to _isdigit
 *
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	if (isdigit != 0)
		return (1);
	return (0);
}
